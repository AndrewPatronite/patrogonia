import { HttpStatus } from './types';
import {
  castSpell,
  createAccount,
  getPlayer,
  getPlayers,
  loadSave,
  login,
  updatePlayer,
} from './playerService';
import { getNpcs, updateNpc } from './npcService';

export {
  castSpell,
  createAccount,
  getNpcs,
  getPlayer,
  getPlayers,
  HttpStatus,
  loadSave,
  login,
  updateNpc,
  updatePlayer,
};
